require('dotenv').config();

module.exports = {
  // Aquí puedes agregar otras variables de configuración
  // Como el email de la empresa para notificaciones.
  email_empresa: process.env.EMAIL_EMPRESA,
};
